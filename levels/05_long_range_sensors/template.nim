# player.nim - Level 5: Long-Range Sensors
# Write your robot AI here and run './discard check' to test.

import discard_api

# TODO: Implement this helper procedure using the implicit 'result' variable!
proc isThreat(bot: Bot, dist: int): bool =
  # Replace 'discard' with: if bot.look(dist, Forward) == Slug: result = true
  discard

proc playTurn*(bot: var Bot) =
  # TODO: Use your helper 'isThreat(bot, dist)' to detect and shoot slugs at distance 2 or 3!
  discard
