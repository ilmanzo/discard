# player.nim - Level 8: Corrupted Area
# Write your robot AI here and run './discard check' to test.

import discard_api

# TODO: Define your states enum and starting state variable!
# Cycle 1 is SAFE, so start in the moving state.
# type BotState = enum StateWalk, StateWait
# var currentState = StateWalk

proc playTurn*(bot: var Bot) =
  # TODO: Implement your State Machine to safely toggle between waiting/resting and walking!
  
  # Replace 'discard' below with your state machine AI!
  discard
